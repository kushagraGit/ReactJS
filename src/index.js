import React from "react";
import ReactDOM from "react-dom";

const element = <h1> Your first react js page</h1>;

console.log(element);

ReactDOM.render(element, document.getElementById('root'));